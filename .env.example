# 公開URL（OG・canonical 用。未設定時は https://viewtrace.net）
NEXT_PUBLIC_SITE_URL=https://viewtrace.net

# Supabase（Dashboard → Project Settings → API）
# anon / public キーはフロントに載る前提。service_role は絶対に NEXT_PUBLIC にしないこと。
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Authentication → URL configuration:
# - Site URL: 本番オリジン（例 https://viewtrace.net）
# - Redirect URLs に {Site URL}/auth/callback と http://localhost:3000/auth/callback を追加
# - Email プロバイダを有効化。ローカルですぐ試す場合は「Confirm email」をオフにできる

# LP「無料トライアル」登録: supabase/migrations の trial_signups を Supabase で実行してから利用

# Resend（https://resend.com/api-keys）
# RESEND_API_KEY=re_xxxx
# 送信元（自ドメインを Resend で検証後、例: Viewtrace <info@viewtrace.net>）
# RESEND_FROM=Viewtrace <info@viewtrace.net>
# POST /api/resend/send 用の共有シークレット（Bearer と完全一致させる長いランダム文字列）
# RESEND_SEND_SECRET=
